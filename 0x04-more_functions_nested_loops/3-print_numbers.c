#include <stdio.h>
#include "main"

/**
 * pritn_numbers - Prints the numbers from 0-9
 *
 * Return: nothing
 */
void print_numbers(void)
{
	int num;

	for (num = 48; num < 58; num++)
	{
		putchar(n);
	}
	putchar(10);
}
