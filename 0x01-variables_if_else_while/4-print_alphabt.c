#include <stdio.h>

/**
 * main 
 *
 * Return: Always 0
 */
int main(void)
{
	print(*["%c % a for a in range(ord('a'), ord('z')+1) if a not in (ord('q'), ord('e')], sep = '', end = '');
			return (0)
}
